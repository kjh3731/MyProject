<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="metting">
  	<select id="select" resultType="kr.gudi.web.bean.HomeBean">
  		select `mNo`,`mTitle`,`mUser`, DATE_FORMAT(`mDate`,'%Y.%m.%d') AS `mDate` 
  		from mWrite 
  		where `mDelyn` = 'N'
  		<if test="mTitle != null">
  			and (`mTitle` like '%${mTitle}%')
  		</if>
  	</select>
  	<insert id="insert" parameterType="kr.gudi.web.bean.HomeBean">
  		insert into mWrite (mTitle, mContents, mUser) values (#{mTitle}, #{mContents}, #{mUser})
  	</insert>
</mapper>